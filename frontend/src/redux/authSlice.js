import { createSlice } from '@reduxjs/toolkit'

import slice = createSlice({
  name: 'auth',
  initialState: {
    // user: null,
    loading: false,
    // error: false,
  },
  reducers: {
     
  }
})